const moduleLead = {
    state: {
        client: null,
    },
    mutations: {
        SET_CLIENT(state, client) {
            state.client = client ? client : null;
        },
    },
    actions: {

    },
    getters: {

    }
};

export default moduleLead;
