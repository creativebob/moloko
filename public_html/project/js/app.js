$(document).foundation();

// Делаем поиск ссылки с атрибутом href = наш фрагмент
// activelem = $('li a[href = "' + link_active + '"]')[0];

// // Берем родителя ссылки - то бишь нам нужен элемент li
// activelem = $(activelem).parent();

// // Прикручиваем к этому элементу класс active
// $(activelem).addClass("active");